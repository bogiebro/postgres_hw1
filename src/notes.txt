Local Buffer Stuff
	- why can we compile without changing it?
	LocalBufferAlloc
	LocalBufferDescriptors
	nextFreeLocalBuf

SpinLocks / LockBufHdr
	- when do we need to use them, if ever?
	- how do we use them without deadlocking?
